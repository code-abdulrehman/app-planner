# Server Configuration
PHX_SERVER=true
PORT=4006
PHX_HOST=localhost

# Database Configuration (Development/Local)
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_HOSTNAME=localhost
DB_NAME=app_planner_dev
POOL_SIZE=10

# Database Configuration (Production/URL)
# DATABASE_URL=ecto://USER:PASS@HOST:PORT/DATABASE
# ECTO_IPV6=false

# Secrets configuration
# You can generate a new one by running: mix phx.gen.secret
SECRET_KEY_BASE=IXXXmcXRLyHW9UdvvSWeG/uSVz1n8mvD/61deIF4kaUJqDDL5i0WGS+9by2ss2dL

# Feature Flags / Environment Modes
# Set to 'true' or '1' to use the DB_* variables above instead of DATABASE_URL in production mode
# USE_LOCAL_DB=false

# Cluster configuration (Optional)
# DNS_CLUSTER_QUERY=
